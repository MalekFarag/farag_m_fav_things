const config = {
    host: "localhost",
    port: 3000, // 8889 for mac, 3000 for windows
    uname: "root",
    pword: "", // leave blank for windows, "root" for mac
    database: "" //link db here
}

module.exports = config;